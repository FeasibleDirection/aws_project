/** Shared infra naming + config constants. */
export const APP_NAME = "aws-crud-demo";
export const DEFAULT_REGION = "us-east-1";

/** Powertools service name injected into every Lambda. */
export const SERVICE_NAME = "orders-api";
